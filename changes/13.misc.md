Typed the whole package so that `ty check ./src/seqtante` passes.

- Added `ty` to the `dev` dependency group and annotated the codebase: return types on the `FittingClass` model functions, class-level attribute declarations on the fit classes (replacing the `self.attr = None` blocks in `__init__`), `TypedDict`s for the `storage` section of the calibration config, and an `ExperimentFunction` protocol describing the signature every entry of `experiment_functions_dict` must implement.
